apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argo-test-apps-stagging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 
    targetRevision: main
    path: apps/argo-test-apps
    helm:
      valueFiles:
        - values/stagging-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argo-test-apps-stagging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
